// 取得當前選中呈現的 Product
export function getCurrentDisplayProduct (state) {
  return state.current_display_product;
}

// 取得當前選中的 Product
export function getCurrentSelectedProduct (state) {
  return state.current_selected_product;
}

// 取得當前選中的 Record ID
export function getCurrentDisplayRecordID (state) {
  return state.current_display_record_id;
}

