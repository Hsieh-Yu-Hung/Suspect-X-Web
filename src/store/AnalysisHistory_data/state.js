/* 設定 state */
export default function () {
  return {
    // 當前 "顯示" 的 Product
    current_display_product: null,
    // 當前 "選中" 的 Record ID
    current_display_record_id: null,
    // 當前 "選中" 的 Product
    current_selected_product: null,
  }
}


