// 設定當前選中呈現的 Product
export function set_current_display_product (state, current_display_product) {
  state.current_display_product = current_display_product;
}

// 設定當前選中的 Record ID
export function set_current_display_record_id (state, current_display_record_id) {
  state.current_display_record_id = current_display_record_id;
}

// 設定當前選中的 Product
export function set_current_selected_product (state, current_selected_product) {
  state.current_selected_product = current_selected_product;
}
